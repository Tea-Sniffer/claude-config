---
description: Investigate a question about the codebase using the codebase-explorer subagent
argument-hint: <question, e.g. "where is price calculation done?">
---

Use the **codebase-explorer** subagent to answer this question about the
codebase:

$ARGUMENTS

Return the answer in the subagent's standard format: lead with the answer,
then evidence with file/line references, then caveats if any.
